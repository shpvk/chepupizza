import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import Header from '../../components/header/header'
import { useAuth } from '../../context/useAuth'
import './Login.css'
function SignUp(){const navigate=useNavigate();const{register}=useAuth();const[username,setUsername]=useState('');const[password,setPassword]=useState('');const[status,setStatus]=useState({type:'',message:''});const[isSubmitting,setIsSubmitting]=useState(false);async function handleSubmit(event){event.preventDefault();if(!username.trim()||!password){setStatus({type:'error',message:'Enter username and password.'});return}setIsSubmitting(true);setStatus({type:'',message:''});try{await register({username:username.trim(),password});navigate('/',{replace:true})}catch{setStatus({type:'error',message:'Could not create account. Try another username.'})}finally{setIsSubmitting(false)}}return <div className="login-page"><Header/><div className="login-card"><div className="login-card__body"><h1 className="login-card__title">Create account</h1><p className="login-card__subtitle">Sign up to place orders faster.</p><form className="login-form" onSubmit={handleSubmit} noValidate><div className="login-form__group"><label className="login-form__label" htmlFor="signup-username">Username</label><input id="signup-username" className="login-form__input" type="text" value={username} onChange={(event)=>setUsername(event.target.value)} autoComplete="username" required/></div><div className="login-form__group"><label className="login-form__label" htmlFor="signup-password">Password</label><input id="signup-password" className="login-form__input" type="password" placeholder="password" value={password} onChange={(event)=>setPassword(event.target.value)} autoComplete="new-password" required/></div>{status.message&&<p className={`login-form__status login-form__status--${status.type}`} role="status">{status.message}</p>}<button className="login-form__btn" type="submit" disabled={isSubmitting}>{isSubmitting?'Creating...':'Create account ->'}</button></form><p className="login-card__signup">Already have an account? <Link className="login-card__signup-link" to="/login">Sign in</Link></p></div></div></div>}
export default SignUp